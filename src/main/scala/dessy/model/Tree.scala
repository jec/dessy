package dessy.model

import dessy.tree.Step

/**
 * Represents a decision tree
 *
 * A Tree "owns" not only the nodes of its decision tree but also all of the
 * related ValueTypes and Values.
 */
trait Tree {
}
